services:
  - type: web
    name: orders-site
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      # PORT est défini automatiquement par Render, pas besoin de le spécifier


